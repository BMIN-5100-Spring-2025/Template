services:
  app:
    image: template:0.0.1
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - dev.env
    volumes:
      - ./data/input:/data/input:ro
      - ./data/output:/data/output
